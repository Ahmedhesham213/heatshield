export function riskTone(score: number) { return score >= 85 ? 'Extreme' : score >= 70 ? 'Very high' : score >= 55 ? 'High' : score >= 35 ? 'Moderate' : 'Low' }
export function routeRecommendation(routes: Array<{ durationMinutes: number; averageTemperature: number; recommended?: boolean }>) { const best = routes.find((route) => route.recommended) ?? [...routes].sort((a, b) => a.averageTemperature - b.averageTemperature)[0]; return best ? `Recommended Route: ${best.durationMinutes - Math.min(...routes.map((route) => route.durationMinutes))} minutes longer, but approximately ${Math.max(0, Math.round(Math.max(...routes.map((route) => route.averageTemperature)) - best.averageTemperature))}°C cooler.` : '' }
